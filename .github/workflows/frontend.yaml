name: frontend workflow
on:
    push:
        paths-ignore:
            - '.github/workflows/**'
        branches: 
            - main
jobs:
    angular:
        runs-on: ubuntu-latest

        steps:
            - name: print somethings
              run: echo "deploying angular"